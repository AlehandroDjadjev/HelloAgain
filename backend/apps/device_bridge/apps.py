from django.apps import AppConfig


class DeviceBridgeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.device_bridge"
    label = "device_bridge"
    verbose_name = "Device Bridge"
